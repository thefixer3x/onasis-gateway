$kind: http-request
description: This endpoint creates one or more image collections (lightboxes). To add images to the collections, use `POST /v2/images/collections/{id}/items`.
url: "{{baseUrl}}/v2/images/collections"
method: POST
headers:
  User-Agent: ShutterstockWorkspace/1.2.0
body:
  type: text
  content: |-
    {
        "name": "<string>"
    }
auth:
  type: oauth2
examples: ./.resources/Create image collections.resources/examples
order: 1000
