$kind: http-request
description: This endpoint creates one or more collections (clipboxes). To add videos to collections, use `POST /v2/videos/collections/{id}/items`.
url: "{{baseUrl}}/v2/videos/collections"
method: POST
headers:
  User-Agent: ShutterstockWorkspace/1.2.0
body:
  type: text
  content: |-
    {
        "name": "<string>"
    }
auth:
  type: oauth2
examples: ./.resources/Create video collections.resources/examples
order: 1000
