$kind: http-request
description: This endpoint returns a list of all audio instruments.
url: "{{baseUrl}}/v2/audio/instruments"
method: GET
headers:
  User-Agent: ShutterstockWorkspace/1.2.0
queryParams:
  - key: language
    value: <string>
    disabled: true
    description: Which language the instruments will be returned in
auth:
  type: basic
  credentials:
    username: <Basic Auth Username>
    password: <Basic Auth Password>
examples: ./.resources/List audio instruments.resources/examples
order: 3000
